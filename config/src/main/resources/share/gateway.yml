server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#      routes:
#      - id: event_route
#        uri: http://jerry.wernerds.net
#        predicates:
#        - Path=/api/manage/*